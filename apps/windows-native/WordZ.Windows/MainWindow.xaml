<Window
    x:Class="WordZ.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="WordZ">
    <NavigationView
        x:Name="ShellNavigation"
        IsBackButtonVisible="Collapsed"
        IsSettingsVisible="False"
        OpenPaneLength="180"
        PaneDisplayMode="Left"
        PaneTitle="WordZ"
        SelectionChanged="OnNavigationSelectionChanged">
        <NavigationView.MenuItems>
            <NavigationViewItem x:Name="LibraryNavItem" Content="&#x672C;&#x5730;&#x8BED;&#x6599;&#x5E93;" Tag="library" />
            <NavigationViewItem Content="&#x7EDF;&#x8BA1;" Tag="stats" />
            <NavigationViewItem Content="KWIC" Tag="kwic" />
            <NavigationViewItem Content="Collocate" Tag="collocate" />
            <NavigationViewItem Content="Compare" Tag="compare" />
            <NavigationViewItem Content="Chi-square" Tag="chi-square" />
            <NavigationViewItem Content="WordCloud" Tag="word-cloud" />
            <NavigationViewItem Content="Ngram" Tag="ngram" />
            <NavigationViewItem Content="Locator" Tag="locator" />
        </NavigationView.MenuItems>

        <Grid Padding="12" RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Padding="8" BorderBrush="#FFD0D0D0" BorderThickness="1">
                <Grid ColumnSpacing="10" RowSpacing="6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Column="0" Spacing="2">
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="WordZ Windows Native" />
                        <TextBlock Text="{Binding Shell.VersionText}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Spacing="2">
                        <TextBlock FontWeight="SemiBold" Text="Engine" />
                        <TextBlock Text="{Binding Shell.EngineStatus}" TextWrapping="Wrap" />
                    </StackPanel>

                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="0"
                        Text="{Binding Shell.WorkspaceSummary}"
                        TextWrapping="Wrap" />
                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="1"
                        Text="{Binding Shell.RuntimeSummary}"
                        TextWrapping="Wrap" />
                </Grid>
            </Border>

            <ContentControl
                x:Name="PageHost"
                Grid.Row="1"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch" />
        </Grid>
    </NavigationView>
</Window>
