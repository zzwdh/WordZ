# Shared

Owns truly cross-domain support.

- Put code here only when it is reused by multiple domains and does not clearly belong to one of them.
- Current shared support includes localization and text decoding helpers.

If a helper primarily serves one domain, move it back into that domain instead of growing `Shared`.
